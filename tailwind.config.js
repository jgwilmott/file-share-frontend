module.exports = {
    content: ["./src/**/*.{ts,tsx}"],
    darkMode: 'media', // or 'media' or 'class'
    theme: {
        extend: {
            colors: {
                yellow: {
                    'light': '#F3F7FD'
                }
            }
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
